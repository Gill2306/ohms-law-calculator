# ohms-law-calculator# 
Ohm's Law Calculator in C

A simple C program that calculates:

- Current (I = V / R)
- Voltage (V = I × R)
- Resistance (R = V / I)

## How to Run

Compile:

```bash
gcc ohms_law.c -o ohms.
./ohms
